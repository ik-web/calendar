import { Box, styled } from '@mui/material';

export const CalendarHead = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  padding: '8px 0',
});

export const CalendarMenu = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  gap: '12px',
});
